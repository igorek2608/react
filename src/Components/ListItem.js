import React,{useState} from 'react'

function ListItem({title, id}) {
    
   
 




    return (
        <div>
            {`${title} ${id}  `} 
            
            
        </div>
    )
}

export default ListItem
